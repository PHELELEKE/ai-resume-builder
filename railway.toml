
[build]
builder = "NIXPACKS"

[phases.setup]
nixPkgs = ["dotnet-sdk_8", "dotnet-runtime_8"]

[phases.build]
cmds = ["dotnet publish -c Release -o out"]

[phases.start]
cmd = "bash -lc 'export ASPNETCORE_URLS=http://0.0.0.0:$PORT && dotnet out/AIResumeBuilder.dll'"
